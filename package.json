{
  "name": "ticket-sniper-standalone-ui",
  "version": "3.3.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node standalone-server.mjs",
    "check": "node --check standalone-server.mjs && node --check runtime-diagnostics.js",
    "verify": "npm run check && npm test",
    "test": "node --test tests/*.test.mjs date-parser.test.cjs"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "playwright-core": "^1.61.1"
  }
}
